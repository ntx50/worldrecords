import * as React from 'react';
import { TableRow, RowState, SingleRowAction, ComponentProps } from './types';
declare type TableCellCheckboxProps<T> = {
    keyField: string;
    row: T;
    rowCount: number;
    selected: boolean;
    selectableRowsComponent: 'input' | React.ReactNode;
    selectableRowsComponentProps: ComponentProps;
    selectableRowsSingle: boolean;
    selectableRowDisabled: RowState<T>;
    onSelectedRow: (action: SingleRowAction<T>) => void;
};
declare function TableCellCheckbox<T extends TableRow>({ keyField, row, rowCount, selected, selectableRowsComponent, selectableRowsComponentProps, selectableRowsSingle, selectableRowDisabled, onSelectedRow, }: TableCellCheckboxProps<T>): JSX.Element;
export default TableCellCheckbox;
